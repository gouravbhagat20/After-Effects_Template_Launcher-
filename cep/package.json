{
  "name": "bighappy-launcher-cep",
  "private": true,
  "description": "Headless test harness for the BigHappy Launcher CEP extension (no runtime dependencies).",
  "scripts": {
    "test": "node --test"
  }
}
